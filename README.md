# Rclone-Automation-Suite
A suite of advanced shell scripts to automate and enhance rclone for robust, unattended file synchronization. Features a two-tier locking system to prevent conflicts, safety checks for open files (lsof), and automatic error recovery. Includes scripts for bisync and sync with desktop notifications and auto-managed logs.
